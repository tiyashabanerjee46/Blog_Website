<%- include('partials/header') -%>

<h1>Home</h1>
<p><%= home %></p>
<% comparray.forEach(function(element){ %>
    <h1><%= element.title %></h1>
    <!-- <% var post = element.post; %>
    <% if(post.length>100) %>
    <% post = post.substr(0,97)+"..."; %> -->
    <p><%= element.post.substr(0,100)+"..." %>
    <a href="/<%= element._id %>">Read more</a></p>



<% }) %>



<%- include('partials/footer') -%>